/**
 * Environment de producción. `apiUrl` queda como placeholder hasta que
 * exista un dominio real de despliegue — ver `MODULOS_PENDIENTES.md` >
 * "Preparación para producción".
 */
export const environment = {
  production: true,
  apiUrl: 'https://api.goods.example.com/api',
};
