/**
 * Environment de producción. `apiUrl` queda como placeholder hasta que
 * exista un dominio real de despliegue — mismo criterio que
 * `admin/src/environments/environment.production.ts`.
 */
export const environment = {
  production: true,
  apiUrl: 'https://api.goods.example.com/api',
};
